import styled from "styled-components";
import { Button } from "../shared/button-styles";
import { ModalHeader, ButtonContainer } from "../MovieModal/MovieModalStyles";

export const DeleteHeader = styled(ModalHeader)``;
export const DeleteButton = styled(Button)``;
export const ConfirmContainer = styled(ButtonContainer)``;
export const DeleteModalQuestion = styled.p`
  color: white;
  font-size: 1.8rem;
  margin-top: 20px;
`;
export const DeleteModalContainer = styled.div`
  margin: 0 3rem;
  padding-top: 20px;
  position: "relative";
`;
